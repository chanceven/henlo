import 'package:flutter/material.dart';

class PawtnerRatingsScreen extends StatelessWidget {
  const PawtnerRatingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ratings')),
      body: const Center(child: Text('Ratings Page')),
    );
  }
}
